import React, { Component } from 'react';
import { connect } from 'react-redux';

class PhotoForm extends Component {
  render() {
    return (
      <div>
        <div>PhotoForm</div>
      </div>
    );
  }
}

export default connect(null, {})(PhotoForm);
